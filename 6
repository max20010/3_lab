#include "stdio.h"
#include "math.h"
int main()
{
	int x,a;
	scanf("%d", &x);
	x=pow(x,x);
	for (int i = 1;i <= x;i++) {
		for (int j = 1;j <= x;j++) {
			a=i*j;
			printf("%d\t", a);
		}
		printf("\n");
	}
	return 0;
}
